import type {Metadata} from "next";import {headers} from "next/headers";import "./globals.css";
export async function generateMetadata():Promise<Metadata>{const h=await headers(),host=h.get("host")??"localhost:3000",scheme=h.get("x-forwarded-proto")??(host.startsWith("localhost")?"http":"https"),image=`${scheme}://${host}/og.png`;return{title:"LinkCheck — Excel Broken Link Checker",description:"上載 Excel，批次檢查 URL 狀態並下載完成版。",icons:{icon:"/favicon.svg"},openGraph:{title:"LinkCheck",description:"Excel Broken Link Checker",images:[image]},twitter:{card:"summary_large_image",title:"LinkCheck",description:"Excel Broken Link Checker",images:[image]}}}
export default function Layout({children}:{children:React.ReactNode}){return <html lang="zh-Hant"><body>{children}</body></html>}